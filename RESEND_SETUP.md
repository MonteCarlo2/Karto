# ⚠️ ВАЖНО: Resend не поддерживает SMTP!

## Проблема

Resend работает **только через API** (Node.js SDK), а Supabase нужен **SMTP протокол**. 

Resend **НЕ МОЖЕТ** быть настроен в Supabase через SMTP Settings, потому что они не предоставляют SMTP сервер.

## ✅ Решение: Используйте SMTP2GO

Вместо Resend используйте **SMTP2GO** - он поддерживает SMTP и очень прост в настройке.

**Смотрите:** `SMTP2GO_QUICK_SETUP.md` - там пошаговая инструкция (5 минут настройки).

---

## Альтернатива: Использовать Resend через API (сложнее)

Если всё-таки хотите использовать Resend, нужно:
1. Создать API route в Next.js
2. Перехватывать события Supabase
3. Отправлять письма через Resend SDK

Но это **намного сложнее** и требует изменения кода. Проще использовать SMTP2GO.

---

## Старая инструкция (не работает для SMTP):

**API ключ:** `re_HxkVh9CF_2dBD1f3qZvTMBHpLd6LJEsmC`

---

## Шаг 1: Верификация sender email (ВАЖНО!)

Resend требует подтверждения email адреса отправителя перед отправкой писем.

### Вариант А: Верификация одного email (быстро)

1. Зайдите на https://resend.com/
2. Войдите в аккаунт
3. Зайдите в **Emails** → **Domains** (или **Settings** → **Sender Authentication**)
4. Нажмите **"Add Domain"** или **"Verify Email"**
5. Выберите **"Verify a Single Email"** (если есть такая опция)
6. Введите ваш email на Рамблере: `karto.help@ro.ru` (или какой у вас)
7. Проверьте почту и подтвердите

**Если нет опции "Verify a Single Email":**
- Используйте **Sandbox Domain** от Resend (для тестирования)
- Или добавьте домен `ro.ru` / `rambler.ru` (но это сложнее, нужны DNS записи)

### Вариант Б: Использовать Sandbox Domain (для начала)

Resend предоставляет тестовый домен для разработки:

1. В Resend зайдите в **Emails** → **Domains**
2. Найдите **Sandbox Domain** (обычно что-то вроде `sandbox-xxx.resend.com`)
3. Можно использовать его для тестирования

**Но лучше верифицировать ваш email!**

---

## Шаг 2: Настройка в Supabase

1. Зайдите в **Supabase Dashboard** → **Settings** → **Authentication** → **SMTP Settings**

2. Включите **"Enable Custom SMTP"** (переключатель должен быть зелёным)

3. Заполните настройки:

```
SMTP Host: smtp.resend.com
SMTP Port: 587
SMTP User: resend
SMTP Password: re_HxkVh9CF_2dBD1f3qZvTMBHpLd6LJEsmC
Sender Email: karto.help@ro.ru (ваш email на Рамблере)
Sender Name: KARTO
```

4. Нажмите **"Save"**

---

## Шаг 3: Проверка работы

1. Сохраните настройки в Supabase
2. Попробуйте зарегистрировать тестового пользователя на `/login`
3. Проверьте логи в Supabase: **Logs** → **Auth Logs**
4. Проверьте почту (включая папку "Спам")

---

## ⚠️ Если письма не отправляются

### Проблема 1: "Sender email not verified"

**Решение:**
- В Resend зайдите в настройки и верифицируйте ваш email `karto.help@ro.ru`
- Проверьте почту и подтвердите

### Проблема 2: "Authentication failed"

**Решение:**
- Убедитесь, что в Supabase:
  - SMTP User: `resend` (именно так, без изменений)
  - SMTP Password: ваш API ключ (полностью скопирован)
- Проверьте, что нет пробелов в начале/конце пароля

### Проблема 3: Письма попадают в спам

**Решение:**
- Это нормально для тестовых доменов
- После верификации вашего email ситуация улучшится

---

## 🔧 Альтернатива: Использовать Sandbox Domain

Если не можете верифицировать `@ro.ru`, можно временно использовать Sandbox Domain от Resend:

1. В Resend найдите ваш Sandbox Domain (например, `sandbox-abc123.resend.com`)
2. В Supabase укажите:
   ```
   Sender Email: onboarding@ваш-sandbox-domain.resend.com
   ```
3. Это для тестирования, потом лучше верифицировать свой email

---

## ✅ Готово!

После настройки регистрация должна работать, и письма будут приходить от вашего email на Рамблере через Resend!
