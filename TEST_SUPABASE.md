# Тест подключения к Supabase

## Проверка настроек

1. ✅ SQL скрипт выполнен успешно в Supabase Dashboard
2. ✅ В `.env.local` добавлены:
   - `SUPABASE_URL=https://ntdnxdccpkpabavgxizd.supabase.co`
   - `SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

## Важно: Перезапуск dev сервера

**КРИТИЧЕСКИ ВАЖНО:** После добавления переменных в `.env.local` нужно:
1. Остановить dev сервер (Ctrl+C)
2. Запустить заново: `npm run dev`

Next.js загружает переменные окружения только при старте, поэтому изменения в `.env.local` не применяются без перезапуска.

## Проверка подключения

После перезапуска dev сервера:
1. Откройте `/studio/understanding`
2. Введите название товара
3. Нажмите "Принять и продолжить"
4. Проверьте консоль сервера - должны быть логи:
   - `✅ Supabase клиент создан: https://ntdnxdccpkpabavgxizd...`
   - `🔄 Сохранение данных этапа 'Понимание'...`
   - `📋 Получены данные: ...`

Если всё правильно, вы должны быть перенаправлены на `/studio/description`.

## Возможные проблемы

### Ошибка 500 при сохранении
- Проверьте, что dev сервер перезапущен
- Проверьте логи в консоли сервера
- Проверьте, что таблицы созданы в Supabase Dashboard → Table Editor

### Ошибка "SUPABASE_URL не установлен"
- Убедитесь, что `.env.local` в корне проекта
- Убедитесь, что нет пробелов вокруг `=` в `.env.local`
- Перезапустите dev сервер

### Ошибка "permission denied"
- Проверьте, что используете `service_role` ключ, а не `anon` ключ
- Проверьте, что RLS политики созданы (должны быть видны в Supabase Dashboard → Authentication → Policies)
