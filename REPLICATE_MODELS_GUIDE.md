# Справочник по моделям Replicate

Этот документ содержит информацию о моделях Replicate, которые можно использовать в проекте.

## 📋 Содержание

1. [Vision модели (распознавание изображений)](#vision-модели)
2. [LLM модели (текстовая генерация)](#llm-модели)
3. [Генерация изображений](#генерация-изображений)
4. [Обработка видео](#обработка-видео)
5. [Другие полезные модели](#другие-модели)

---

## Vision модели

Модели для анализа изображений, распознавания объектов, генерации описаний.

### Текущие используемые

- **`yorickvp/llava-13b`** — используется для распознавания товаров
  - **Input:** `image` (URL), `prompt` (строка), `max_tokens` (число)
  - **Output:** текст описания
  - **Использование:** `recognizeProduct()` в `replicate.ts`

### Рекомендуемые альтернативы

- **`google/gemini-2.5-flash`** — быстрая vision модель от Google
  - Очень хорошее качество
  - Может работать из России
  - **Input:** `image`, `prompt`
  - **Output:** текст

- **`openai/gpt-4o-mini`** — vision модель от OpenAI (через Replicate)
  - Отличное качество распознавания
  - **Может работать через Replicate даже если OpenAI API заблокирован**
  - **Input:** 
    - `messages` (массив сообщений):
      - `role: "system"` с `content` (системный промпт)
      - `role: "user"` с `content` (массив объектов):
        - `{ type: "image_url", image_url: { url: "..." } }`
        - `{ type: "text", text: "..." }`
    - `temperature` (число, 0.0-2.0, рекомендуется 0.2 для точности)
    - `max_completion_tokens` (число, рекомендуется 300)
  - **Output:** массив строк или строка
  - **ВАЖНО:** Использует формат `messages`, а не просто `prompt` и `image`!
  - **Использование:** `getProductNamesFromReplicateGPT4oMini()` в `replicate.ts`
  - **Приоритет:** Высокий (второй после прямого OpenAI API)

- **`anthropic/claude-4-sonnet`** — vision модель от Anthropic
  - Высокое качество
  - **Input:** `image`, `prompt`
  - **Output:** текст

- **`lucataco/moondream2`** — легковесная vision модель
  - Быстрая и дешёвая
  - **Input:** `image`, `prompt`
  - **Output:** текст описания

---

## LLM модели

Модели для генерации текста, названий, описаний.

### Текущие используемые

- **`meta/meta-llama-3-8b-instruct`** — используется для генерации названий
  - **Input:** 
    - `prompt` (строка)
    - `max_new_tokens` (число)
    - `temperature` (число, 0.0-1.0)
    - `prompt_template` (строка, специальный формат для Llama 3)
    - `system_prompt` (строка, опционально)
  - **Output:** текст
  - **Формат промпта:** требует специальный `prompt_template` с токенами `<|begin_of_text|>`, `<|start_header_id|>`, и т.д.
  - **Использование:** `generateProductNamesFromDescription()` в `replicate.ts`

- **`meta/meta-llama-3-70b-instruct`** — более мощная версия
  - Те же параметры, что и 8B
  - Лучше следует инструкциям
  - Дороже и медленнее

- **`mistralai/mixtral-8x7b-instruct-v0.1`** — fallback модель
  - **Input:** `prompt`, `max_tokens`, `temperature`
  - **Output:** текст
  - Не требует специального формата промпта

- **`mistralai/mistral-7b-instruct-v0.2`** — fallback модель
  - **Input:** `prompt`, `max_tokens`, `temperature`
  - **Output:** текст

### Другие полезные LLM

- **`openai/gpt-5`** — новейшая модель OpenAI
  - **Не работает из России**
  - Очень высокое качество

- **`openai/gpt-4.1-nano`** — легковесная версия GPT-4
  - Быстрая и дешёвая
  - **Не работает из России**

- **`meta/llama-2-70b-chat`** — используется для генерации текста товара
  - **Input:** `prompt`, `max_tokens`, `temperature`
  - **Output:** текст
  - **Использование:** `generateProductText()` в `replicate.ts`

---

## Генерация изображений

### Текущие используемые

- **`stability-ai/sdxl`** — используется для генерации фона
  - **Input:** 
    - `prompt` (строка)
    - `negative_prompt` (строка)
    - `width`, `height` (числа)
    - `num_outputs` (число)
    - `scheduler` (строка, например "K_EULER")
    - `num_inference_steps` (число)
    - `guidance_scale` (число)
  - **Output:** массив URL изображений
  - **Использование:** `generateBackground()`, `inpaintBackground()` в `replicate.ts`

### Рекомендуемые альтернативы

- **`black-forest-labs/flux-2-max`** — одна из лучших моделей генерации
  - Очень высокое качество
  - **Input:** `prompt`, `aspect_ratio`, `output_format`, и др.
  - **Output:** URL изображения

- **`google/nano-banana-pro`** — используется для генерации карточек
  - **Input:** `prompt`, `aspect_ratio`, `output_format`, `resolution`, `image_input` (опционально)
  - **Output:** URL изображения
  - **Использование:** `generateWithNanobanana()` в `nanobanana.ts`

- **`google/imagen-4`** — модель от Google
  - Высокое качество
  - **Input:** `prompt`, и др.
  - **Output:** URL изображения

---

## Обработка видео

### Текущие используемые

- **`topazlabs/video-upscale`** — используется для апскейла видео
  - **Input:** `video` (URL), `scale` (число)
  - **Output:** URL видео
  - **Использование:** `upscaleVideoTo4K()` в `video-upscale.ts`

### Другие полезные

- **`openai/sora-2`** — генерация видео из текста
  - **Input:** `prompt`, и др.
  - **Output:** URL видео

- **`kwaivgi/kling-v2.5-turbo-pro`** — генерация видео из изображений
  - **Input:** `image`, `prompt`, и др.
  - **Output:** URL видео

- **`google/veo-3.1`** — генерация видео от Google
  - **Input:** `prompt`, и др.
  - **Output:** URL видео

---

## Другие модели

### Удаление фона

- **`cjwbw/rembg`** — используется для удаления фона
  - **Input:** `image` (URL)
  - **Output:** URL изображения без фона
  - **Использование:** `removeBackground()` в `replicate.ts`

- **`lucataco/remove-bg`** — альтернатива
- **`851-labs/background-remover`** — альтернатива

### Улучшение изображений

- **`philz1337x/crystal-upscaler`** — апскейл изображений
  - **Input:** `image`, `scale`
  - **Output:** URL улучшенного изображения

- **`google/upscaler`** — апскейл от Google
  - **Input:** `image`
  - **Output:** URL

- **`megvii-research/nafnet`** — восстановление изображений
  - **Input:** `image`
  - **Output:** URL восстановленного изображения

### OCR (распознавание текста)

- **`datalab-to/marker`** — OCR для извлечения текста
  - **Input:** `image`
  - **Output:** текст

- **`lucataco/deepseek-ocr`** — альтернатива OCR
- **`bytedance/dolphin`** — альтернатива OCR

### Субтитры для видео

- **`google/gemini-2.5-flash`** — может генерировать субтитры
- **`fictions-ai/autocaption`** — автоматические субтитры
- **`shreejalmaharjan-27/tiktok-short-captions`** — короткие субтитры

### Редактирование изображений

- **`openai/gpt-image-1.5`** — редактирование изображений
- **`prunaai/p-image-edit`** — редактирование
- **`google/nano-banana-pro`** — редактирование с референсом

---

## Формат API вызовов

### Базовый формат

```typescript
import Replicate from "replicate";
const replicate = new Replicate({ auth: process.env.REPLICATE_API_TOKEN });

// Простой вызов
const output = await replicate.run("model-id", {
  input: {
    prompt: "текст",
    max_tokens: 400,
    temperature: 0.1,
  }
});
```

### Стриминг (для LLM)

```typescript
// Для моделей, которые поддерживают стриминг
for await (const event of replicate.stream("model-id", { input })) {
  process.stdout.write(event);
}
```

### Специальный формат для Llama 3

```typescript
const input = {
  prompt: "ваш промпт",
  max_new_tokens: 400,
  temperature: 0.1,
  prompt_template: "<|begin_of_text|><|start_header_id|>system<|end_header_id|>\n\n{system_prompt}<|eot_id|><|start_header_id|>user<|end_header_id|>\n\n{prompt}<|eot_id|><|start_header_id|>assistant<|end_header_id|>\n\n",
  system_prompt: "системный промпт (опционально)"
};
```

---

## Полезные ссылки

- [Replicate Collections](https://replicate.com/collections) — все коллекции моделей
- [Vision Models](https://replicate.com/collections/vision-models) — vision модели
- [LLM Models](https://replicate.com/collections/large-language-models-llm) — LLM модели
- [Replicate API Docs](https://replicate.com/docs) — документация API

---

## Примечания

- Все модели требуют `REPLICATE_API_TOKEN` в `.env.local`
- Некоторые модели (OpenAI, Anthropic) могут быть заблокированы из России
- Для моделей с rate limit добавлены задержки в коде
- Все модели возвращают результат в разном формате — нужно проверять документацию конкретной модели
