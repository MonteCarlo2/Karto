# Быстрая настройка Яндекс OAuth для KARTO

## 🎯 Что нужно сделать

Настроить вход через Яндекс, чтобы пользователи могли регистрироваться и входить через свой Яндекс аккаунт.

---

## Шаг 1: Создайте приложение в Яндекс OAuth

1. **Зайдите на:** https://oauth.yandex.ru/
2. **Нажмите:** "Зарегистрировать новое приложение"
3. **Заполните форму:**
   - **Название приложения:** `KARTO`
   - **Платформы:** выберите **"Веб-сервисы"**
   - **Callback URI #1:** 
     ```
     https://ntdnxdccpkpabavgxizd.supabase.co/auth/v1/callback
     ```
     (Это URL вашего Supabase проекта)
   - **Callback URI #2:** 
     ```
     http://localhost:3000/auth/callback
     ```
     (Для локальной разработки)
4. **Нажмите:** "Создать"

---

## Шаг 2: Получите Client ID и Client Secret

После создания приложения вы увидите:

- **ID приложения** (Client ID) - скопируйте его
- **Пароль приложения** (Client Secret) - скопируйте его (показывается только один раз!)

⚠️ **ВАЖНО:** Сохраните оба значения! Client Secret показывается только один раз!

---

## Шаг 3: Настройте в Supabase

1. **Зайдите в Supabase Dashboard**
2. **Settings** → **Authentication** → **Providers**
3. **Найдите "Yandex"** в списке провайдеров
4. **Включите переключатель** "Enable Yandex provider"
5. **Заполните:**
   - **Client ID (secret):** вставьте ваш Client ID из Яндекс
   - **Client Secret (secret):** вставьте ваш Client Secret из Яндекс
6. **Нажмите:** "Save"

---

## Шаг 4: Проверьте Redirect URLs

1. В том же разделе **Providers** → **Yandex**
2. Убедитесь, что в **"Redirect URLs"** указаны:
   - `https://ntdnxdccpkpabavgxizd.supabase.co/auth/v1/callback`
   - `http://localhost:3000/auth/callback` (для разработки)

Если их нет - добавьте вручную.

---

## ✅ Готово! Проверка работы

1. Зайдите на `/login` в вашем приложении
2. Нажмите **"Продолжить с Яндекс"**
3. Должно открыться окно авторизации Яндекс
4. Войдите в свой Яндекс аккаунт
5. После авторизации вы будете перенаправлены обратно на главную страницу
6. Вы должны быть автоматически залогинены

---

## 🔧 Как это работает

1. Пользователь нажимает "Продолжить с Яндекс"
2. Перенаправляется на Яндекс для авторизации
3. После авторизации Яндекс перенаправляет на Supabase callback
4. Supabase создаёт/обновляет пользователя в базе данных
5. Пользователь перенаправляется на главную страницу (`/`)
6. Пользователь автоматически залогинен

---

## ⚠️ Важно для локальной разработки

Для работы на `localhost:3000`:
- ✅ В Яндекс OAuth добавьте `http://localhost:3000/auth/callback` в Callback URI
- ✅ В Supabase добавьте `http://localhost:3000/auth/callback` в Redirect URLs

---

## ⚠️ Важно для продакшена (когда зальёте на Vercel)

После деплоя на Vercel:

1. **В Яндекс OAuth** добавьте ваш домен в Callback URI:
   - `https://ваш-домен.vercel.app/auth/callback`
   - Или если используете кастомный домен: `https://ваш-домен.com/auth/callback`

2. **В Supabase** добавьте тот же URL в Redirect URLs:
   - Settings → Authentication → Providers → Yandex → Redirect URLs

---

## 🐛 Если не работает

### Проблема 1: "Invalid client" или "Invalid credentials"

**Решение:**
- Проверьте, что Client ID и Client Secret правильные
- Убедитесь, что скопировали их полностью, без пробелов
- Проверьте, что провайдер "Yandex" включен в Supabase

### Проблема 2: "Redirect URI mismatch"

**Решение:**
- Убедитесь, что Callback URI в Яндекс **ТОЧНО** совпадает с Redirect URL в Supabase
- Проверьте, что нет лишних пробелов или символов
- Для локальной разработки используйте `http://localhost:3000/auth/callback` (не `https`)

### Проблема 3: Не перенаправляет обратно на сайт

**Решение:**
- Проверьте, что в Supabase указан правильный Redirect URL
- Проверьте консоль браузера на ошибки
- Убедитесь, что провайдер включен

### Проблема 4: Ошибка в консоли

**Решение:**
- Откройте консоль браузера (F12)
- Посмотрите на ошибки
- Проверьте, что Supabase правильно настроен (NEXT_PUBLIC_SUPABASE_URL и NEXT_PUBLIC_SUPABASE_ANON_KEY)

---

## 📝 Примечания

- ✅ Яндекс OAuth работает **без домена** - можно тестировать на localhost
- ✅ После авторизации пользователь автоматически регистрируется/входит
- ✅ Email пользователя берётся из Яндекс аккаунта
- ✅ Имя пользователя можно получить из данных Яндекс (если нужно)

---

## 🎉 Готово!

Теперь пользователи могут входить через Яндекс одним кликом!
